.env
.venv/
venv/
ENV/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.swp
.DS_Store
logs/
.pytest_cache/
# Runtime state (persistent log file etc.) — keep the dir, ignore contents
data/*
!data/.gitkeep
# Claude Code agent worktrees — never commit
.claude/worktrees/
# Production log snapshots pulled for analysis — contain real user queries
tests/fixtures/prod-*.log
# Rendered production usage reports — contain real per-day counters and visited domains.
# The whole directory is generated dumps: nothing in it is tracked or referenced, and a
# report saved under a different name must not slip through into a public repository.
docs/stats/
